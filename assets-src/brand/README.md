# Brand assets (sources)

The TAPPaaS logo ("A Private Cloud on a plate") — editable Excalidraw source plus
PNG/SVG exports. Moved here from the source repo's `docs/assorted stuff/` during the
TAPPaaS/TAPPaaS#317 cleanup (ADR-013: brand/site assets live in the Documentation repo).

The published copies used by the site live in `docs/` (`tappaas-logo.png`,
`tappaas-icon.png`); regenerate them from `TAPPaaS.excalidraw` when the logo changes.
