# Claude Code Instructions

## Publishing Changes

When asked to publish or push changes:

1. Commit directly to the current branch (typically `main`)
2. Push with `git push`
3. Do **not** create pull requests — the site deploys automatically on push to `main`
