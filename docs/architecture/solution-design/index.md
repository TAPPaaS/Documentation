---
title: Foundation
description: >
  What the TAPPaaS foundation is — the modules it consists of, how they depend
  on each other, and where each part's design lives.
---

# The Foundation

The foundation is what makes TAPPaaS a *platform*: cluster, network, templates,
automation, backup, identity and logging, working as one. Each part's design and
alternatives live with its module — the catalog is under
[Stacks → Foundation](../module-designs/foundation.md), where every module page
carries its architecture diagram, alternatives considered and design depth.

How the software behind each part gets chosen is a principle, not an accident — the
curation criteria live in the [Design Principles](../../intro/design-principles.md)
(principle 4); each module's README records the alternatives that were considered.

## The module model

Everything is delivered as **modules** — the smallest deployable units, each
declaring what it provides and what it depends on in its own json. The dependency
graph is therefore *computed from the modules themselves* (regenerated by
`src/generate-module-dependencies.sh` in the source repo):

[:octicons-arrow-right-24: Module Dependencies](../../generated/module-dependencies.md)
